apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: ac-portal-base

resources:
- deployment.yaml
- service.yaml
- route.yaml  
- configmap.yaml

commonLabels:
  app: ac-portal
  version: v1.0.0

images:
- name: ac-portal
  newTag: latest