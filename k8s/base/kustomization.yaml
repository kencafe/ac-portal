apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: ac-portal-base

resources:
  - imagestream.yaml
  - buildconfig.yaml
  - configmap.yaml
  - networkpolicy.yaml
  - data-pvc.yaml
  - deployment.yaml
  - service.yaml
  - route.yaml
  - cms-auth.yaml
  - blog-route.yaml
  - ai-cron.yaml

commonLabels:
  app: ac-portal
