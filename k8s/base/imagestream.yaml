apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: ac-portal
  labels:
    app: ac-portal
    component: frontend
spec:
  # Tekton pushes ac-portal:<env> here; local lookup lets Deployments
  # reference the tag by name and get the internal registry digest.
  lookupPolicy:
    local: true
