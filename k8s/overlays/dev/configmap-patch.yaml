apiVersion: v1
kind: ConfigMap
metadata:
  name: ac-portal-config
data:
  SITE_URL: "https://dev.appcarrier.cloud"
  # dev is fronted by the oauth2-proxy CMS route → trust its X-Forwarded-* auth
  # headers. NEVER set this on a public env without a proxy (SEC-001).
  TRUST_FORWARDED_AUTH: "1"
